# Task list

- [ ] Slice 1 — index-core: schema v3, migrations, hashing (tests first)
- [ ] Slice 2 — scan-pipeline: exif, labels, scanner rewrite, jobs, GC (tests first)
- [ ] Slice 3 — library-state + collections (tests first)
- [ ] Slice 4 — people v3 (tests first)
- [ ] Slice 5 — search-views: query parser, feeds, places, memories (tests first)
- [ ] Slice 6 — render-pipeline, dispatcher, electron media:// (tests first)
- [ ] Slice 7 — web-ui shell + feed + viewer + editor + pages + mock
- [ ] Slice 8 — acceptance e2e, critic agents, visual judge, ship
