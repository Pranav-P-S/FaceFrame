# Face detection & recognition
# Prebuilt insightface wheels exist for Python 3.10-3.12; on newer Pythons
# pip would have to compile it from source (needs a C++ toolchain).
insightface>=0.7.3,<0.8

# CPU inference by default -- works on Windows, Linux and macOS with no
# extra setup. See the README for optional GPU acceleration.
onnxruntime>=1.16,<2

numpy>=1.26,<2
opencv-python-headless>=4.8
Pillow>=10.0

# Face clustering
scikit-learn>=1.3
